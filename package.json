{
  "name": "mjson-diff",
  "version": "0.0.1",
  "description": "",
  "main": "./bin/mjson-diff",
  "bin": {
    "mjson-diff": "./build/bin/mjson-diff.js"
  },
  "scripts": {
    "format": "git diff-index --quiet HEAD -- && git ls-files **/*.ts | xargs prettier --bracket-spacing false --write || echo 'Cannot format if there are changed files'",
    "tsc": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.50",
    "@types/node": "^7.0.4",
    "prettier": "^0.11.0",
    "typescript": "^2.1.5"
  }
}
