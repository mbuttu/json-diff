{
  "name": "json-diff",
  "version": "0.0.1",
  "description": "",
  "main": "./bin/json-diff",
  "bin": {
    "json-diff": "./bin/json-diff.js"
  },
  "scripts": {
    "format": "git diff-index --quiet HEAD -- && git ls-files **/*.js | xargs prettier --bracket-spacing false --write || echo 'Cannot format if there are changed files'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "prettier": "^0.11.0"
  }
}
